<h3><%= @employee.first_name%> <%= @employee.last_name %></h3>

<p>Alias: <%= @employee.alias %></p>
<p>Title: <%= @employee.title %></p>
<p>Office: <%= @employee.office %></p>
<p>Dog: <%= link_to @employee.dog.name, dog_path(@employee.dog) %></p>
<img src = <%= @employee.img_url %> alt = "Picture of Employee" title = "Picture of Employee">

# <%= button_to "Delete Employer", employee_path(@employee), method: :delete %>
